<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.FinalProject" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/pink</item>
        <item name="colorPrimaryVariant">@color/pinkDark</item>
        <item name="colorOnPrimary">@color/textOnPink</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/pinkLight</item>
        <item name="colorSecondaryVariant">@color/pinkDark</item>
        <item name="colorOnSecondary">@color/textOnPink</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:textColor">@color/text</item>
        <item name="android:textSize">23sp</item>
    </style>
</resources>